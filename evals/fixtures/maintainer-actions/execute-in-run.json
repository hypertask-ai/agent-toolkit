{
  "instructions": [
    "Merge https://github.com/example/allowed/pull/7",
    "Release https://github.com/example/allowed/pull/7 and update the toolkit",
    "Update the installed toolkit without changing timer state",
    "Build the requested toolkit change for this ticket"
  ],
  "required_commands": [
    "agent-template merge <pr-url>",
    "agent-template build",
    "agent-template update --keep-timers"
  ],
  "forbidden_outcomes": [
    "delegate it",
    "assign it to a developer",
    "answer with a handoff"
  ]
}
