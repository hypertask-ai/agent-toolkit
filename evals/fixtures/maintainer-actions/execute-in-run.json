{
  "instructions": [
    "Request a manual merge of https://github.com/example/allowed/pull/7",
    "Request a change to https://github.com/example/allowed/pull/7 labelled valentin-review",
    "Update the installed toolkit without changing timer state",
    "Build the requested toolkit change for this ticket"
  ],
  "required_commands": [
    "Never merge a pull request by hand",
    "pull requests labelled `valentin-review` stay untouched",
    "agent-template build",
    "agent-template update --keep-timers"
  ],
  "forbidden_outcomes": [
    "delegate it",
    "hand it to a developer"
  ]
}
