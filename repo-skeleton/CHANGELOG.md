# __AGENT_NAME__ changelog

One entry per change to this agent's own scripts or reports. The board
ticket the change came from, not a summary of the diff.

## unreleased

- Repo created from the create-agent skeleton (`create-agent.sh --pr-repo`).
